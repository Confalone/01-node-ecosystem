'use strict';   //think this is done  need tests

function add(a,b) {

  if (typeof a === isNaN || typeof b === ) {
    return null;
  }
  return a+b;  // do i need parenteses around a+b
}

function sub(a,b) {

    if (typeof a === isNaN || typeof b === ) {
      return null;
    }
    return a-b;  // do i need parenteses around a+b
  }

module.exports=arithmetic;